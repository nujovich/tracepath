FROM python:3.12-slim
RUN apt-get update && apt-get install -y --no-install-recommends \
    libgit2-dev \
    git \
    && rm -rf /var/lib/apt/lists/*
WORKDIR /app
COPY policies/ /app/
RUN pip install --no-cache-dir pygit2
EXPOSE 9003
CMD ["python3", "-m", "policy_engine.api"]