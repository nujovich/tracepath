# Screenshots

Place the following screenshots here for the challenge submission:

## Required screenshots

1. **`incidents-rate-limit.png`** — Dashboard Incidents tab showing a `rate_limit_breach` incident ("Rate limit breach: 61 calls in 0.0s (limit: 60/min)")

2. **`policies-version-history.png`** — Dashboard Policies tab showing the version history table with multiple versions, Diff → and Rollback buttons

3. **`incidents-suspicious-pattern.png`** — Dashboard Incidents tab showing `suspicious_pattern` warnings for consecutive same-tool usage

4. **`dashboard-audit.png`** — Dashboard Audit tab showing the event table with policy decisions (allowed/denied)

## How to capture

- Dashboard runs at `http://localhost:3000`
- Take full-browser screenshots (not just the viewport)
- Dark mode should be clearly visible
